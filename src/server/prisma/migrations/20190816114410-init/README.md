# Migration `20190816114410-init`

This migration has been generated by mkubara at 8/16/2019, 11:44:10 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "file:dev"."User"("id" TEXT NOT NULL  ,"email" TEXT NOT NULL DEFAULT '' ,"name" TEXT   ,PRIMARY KEY ("id"));

CREATE TABLE "file:dev"."Post"("id" TEXT NOT NULL  ,"createdAt" DATE NOT NULL  ,"updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"deletedAt" DATE   ,"published" BOOLEAN NOT NULL DEFAULT false ,"title" TEXT NOT NULL DEFAULT '' ,"content" TEXT   ,"author" TEXT   REFERENCES "User"(id),PRIMARY KEY ("id"));

CREATE TABLE "file:dev"."Race"("id" INTEGER NOT NULL DEFAULT 0 ,"raceId" TEXT NOT NULL DEFAULT '' ,"createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"raceDate" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"courseCode" TEXT NOT NULL DEFAULT '' ,"kai" INTEGER NOT NULL DEFAULT 0 ,"nichi" INTEGER NOT NULL DEFAULT 0 ,"number" INTEGER NOT NULL DEFAULT 0 ,"hondai" TEXT NOT NULL DEFAULT '' ,"gradeCode" TEXT NOT NULL DEFAULT '' ,"joukenCodeJy" TEXT NOT NULL DEFAULT '' ,"distance" INTEGER NOT NULL DEFAULT 0 ,"trackCode" TEXT NOT NULL DEFAULT '' ,"hassouTime" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"tourokuTousuu" INTEGER NOT NULL DEFAULT 0 ,"shussouTousuu" INTEGER NOT NULL DEFAULT 0 ,PRIMARY KEY ("id"));

CREATE TABLE "file:dev"."UmaRace"("id" INTEGER NOT NULL DEFAULT 0 ,"umaRaceId" TEXT NOT NULL DEFAULT '' ,"createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"raceDate" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"wakuban" INTEGER NOT NULL DEFAULT 0 ,"umaban" INTEGER NOT NULL DEFAULT 0 ,"name" TEXT NOT NULL DEFAULT '' ,"sexCode" TEXT NOT NULL DEFAULT '' ,"keiroCode" TEXT NOT NULL DEFAULT '' ,"age" INTEGER NOT NULL DEFAULT 0 ,"tozaiCode" TEXT NOT NULL DEFAULT '' ,"choukyoushiRyakushou" TEXT NOT NULL DEFAULT '' ,"futanJuuryou" INTEGER NOT NULL DEFAULT 0 ,"kishuRyakushou" TEXT NOT NULL DEFAULT '' ,"odds" INTEGER NOT NULL DEFAULT 0 ,"ninki" INTEGER NOT NULL DEFAULT 0 ,"race" INTEGER NOT NULL  REFERENCES "Race"(id),PRIMARY KEY ("id"));

CREATE UNIQUE INDEX "file:dev"."User.id._UNIQUE" ON "User"("id")

CREATE UNIQUE INDEX "file:dev"."User.email._UNIQUE" ON "User"("email")

CREATE UNIQUE INDEX "file:dev"."Post.id._UNIQUE" ON "Post"("id")

CREATE UNIQUE INDEX "file:dev"."Race.raceId._UNIQUE" ON "Race"("raceId")

CREATE UNIQUE INDEX "file:dev"."UmaRace.umaRaceId._UNIQUE" ON "UmaRace"("umaRaceId")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190816114410-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,72 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+  default  = true
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+generator nexus_prisma {
+  provider = "nexus-prisma"
+}
+
+model User {
+  id    String  @default(cuid()) @id @unique
+  email String  @unique
+  name  String?
+  posts Post[]
+}
+
+model Post {
+  id        String   @default(cuid()) @id @unique
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  deletedAt DateTime?
+  published Boolean
+  title     String
+  content   String?
+  author    User?
+}
+
+model Race {
+  id            Int @id
+  raceId        String @unique
+  createdAt     DateTime
+  raceDate      DateTime
+  courseCode    String
+  kai           Int
+  nichi         Int
+  number        Int
+  hondai        String
+  gradeCode     String
+  joukenCodeJy  String
+  distance      Int
+  trackCode     String
+  hassouTime    DateTime
+  tourokuTousuu Int
+  shussouTousuu Int
+
+  umaRaces      UmaRace[]
+}
+
+model UmaRace {
+  id            Int @id
+  umaRaceId     String @unique
+  race          Race
+  createdAt     DateTime
+  raceDate      DateTime
+  wakuban       Int
+  umaban        Int
+  name          String
+  sexCode       String
+  keiroCode     String
+  age           Int
+  tozaiCode     String
+  choukyoushiRyakushou  String
+  futanJuuryou  Int
+  kishuRyakushou  String
+  odds          Int
+  ninki         Int
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190816114410-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190816114410-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
