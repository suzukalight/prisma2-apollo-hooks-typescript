export const convSex = (code: string) => {
  if (code === '1') return '牡';
  if (code === '2') return '牝';
  return '';
};

export const convCourse = (code: string) => {
  if (code === '1') return '牡';
  if (code === '2') return '牝';
  return '';
};
